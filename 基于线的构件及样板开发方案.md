# 基于线的构件及样板开发方案

## 1、方案目标

本方案旨在构建一套基于样板驱动的线性构件系统，核心能力包括

- 建立统一的"样板几何 → 项目几何"映射机制，支持:
  - 直线布置
  - 圆弧布置
  - 预留扩展能力以支持更多路径类型（如样条曲线、椭圆弧、拾取线等）
- 实现自定义实体，具备如下能力：
  - 动态渲染（基于映射实时生成几何）
  - 夹点编辑（直线/圆弧差异化支持）
  - 通用编辑兼容（移动 / 复制 / 旋转 / 镜像 / stretch）
  - 编辑过程异常检测与失败回退
- 实现完整的构件布置流程：
  - 命令输入
  - Jig 实时预览
  - 可放置判断
  - 合法性校验 -> 异常提示与结果提交。

## 2、技术架构

### 2.1 几何映射框架

#### 2.1.1 编码框架设计

- 布置类型抽象
定义统一的布置类型枚举：

```cpp
enum class LinearCompType : uint8_t
{
    kLine,      // 直线布置
    kArc,       // 圆弧布置
    kSpline,    // 样条曲线布置（拟合点序列定义路径）
    // 预留扩展：kPolyline（拾取线 / 多段线）
};  
```

- 定位线数据结构设计
不同布置方式下的定位线参数差异较大，采用基类 + 派生类结构表达。
基类提供 `type()` 和 `pathLength()` 虚函数，消除下游对 `static_cast` 的依赖，
同时提供 `clone()` 以支持值语义拷贝：

```cpp
struct LineCompGeometry {
    ZcGePoint3d start;
    ZcGePoint3d end;

    double pathLength() const { return start.distanceTo(end); }

    // 按需构造 Ge 曲线（用于 Strategy 映射、几何查询等场景）
    ZcGeLineSeg3d toGeCurve() const { return ZcGeLineSeg3d(start, end); }
};

struct ArcCompGeometry {
    ZcGePoint3d start;
    ZcGePoint3d end;
    ZcGePoint3d center;
    ZcGeVector3d normal;   // 圆弧平面法向，同时隐含弧方向（右手定则）

    double radius() const { return start.distanceTo(center); }

    double sweepAngle() const {
        double r = radius();
        if (r < 1e-10) return 0.0;
        ZcGeVector3d v1 = (start - center).normal();
        ZcGeVector3d v2 = (end - center).normal();
        double cosA = std::clamp(v1.dotProduct(v2), -1.0, 1.0);
        double angle = std::acos(cosA);
        if (v1.crossProduct(v2).dotProduct(normal) < 0.0)
            angle = 2.0 * M_PI - angle;
        return angle;
    }

    double pathLength() const { return radius() * sweepAngle(); }

    // 按需构造 Ge 曲线（用于 Strategy 映射、几何查询等场景）
    ZcGeCircArc3d toGeCurve() const {
        ZcGeVector3d refVec = (start - center).normal();
        return ZcGeCircArc3d(center, normal, refVec, radius(), 0.0, sweepAngle());
    }
};
```

**设计说明**：

- **混合方案（存储分量，按需构造 Ge 类型）**：Geometry 以自定义 struct 存储原始分量（`ZcGePoint3d`、`ZcGeVector3d` 等），序列化/夹点编辑/变换直接操作分量字段；需要几何查询能力时通过 `toGeCurve()` 按需构造对应的 Ge 类型（`ZcGeLineSeg3d`、`ZcGeCircArc3d` 等），不持久化 Ge 对象。
- **不存储 Ge 类型的理由**：Ge 曲线对象是不可变语义的重对象，内部字段相互耦合（如 `ZcGeCircArc3d` 的 center/radius/angle 必须自洽），部分修改（如只移动圆弧起点）需要重新构造整个对象，不利于夹点编辑场景下的逐字段操作。
- **不存储 `ZcDbCurve*` 的理由**：`ZcDbCurve` 是数据库驻留实体，受 undo/redo 事务管理，存在生命周期、嵌套事务、用户可见性、深拷贝等问题，不适合作为自定义实体的内部数据字段。若需引用图纸上已有曲线，应存 `ZcDbObjectId`（软引用），使用时再 open 读取。
- Geometry 是**纯数据 + 几何计算**的轻量结构体，不承担序列化、夹点、变换等实体层职责。序列化/夹点/变换由各自定义实体子类的 `sub*` override 直接处理（见 2.2 节）。
- 不再使用 `ILinearCompGeometry` 多态基类 — 因为实体层已经通过派生来区分类型，Geometry 层再做一层多态属于冗余间接。各实体子类直接持有自己的具体 Geometry 类型。
- `radius` 不再作为独立字段存储，改为 `radius()` 计算方法，消除与 `start`/`center` 的冗余不一致风险。
- `sweepAngle()` 从 `pathLength()` 中提取为独立方法，供 `pathLength()` 和 `toGeCurve()` 复用，避免角度计算逻辑重复。
- `normal` 显式存储圆弧平面法向量，避免运行时从 `start`/`end` 叉积推导时的退化问题（半圆弧时叉积为零向量）。
- `pathLength()` 利用 `normal` 判断弧方向，正确处理优弧/劣弧。

- 映射框架设计
采用：策略模式（Strategy）隔离不同布置方式的映射算法 + 桥接模式（Bridge）解耦"样板几何表达"与"路径映射方式"。

`ILinearCompStrategy` 定义纯虚的 `mapPoint` 作为基础映射原语，在此基础上为曲线、面、体提供**基于采样的默认实现**。子类可选覆盖以提供解析解（更高精度/性能）。各子类直接持有**具体类型**的几何数据（通过 `shared_ptr` 保证生命周期安全），消除 `static_cast`：

```cpp
class ILinearCompStrategy {
public:
    virtual ~ILinearCompStrategy() = default;

    // ─── 基础原语（纯虚）───
    virtual ZcGePoint3d mapPoint(const ZcGePoint3d& pt) const = 0;

    // ─── 曲线映射 ───
    // 默认：采样 N 点 → mapPoint → 拟合 NurbCurve
    // 子类可覆盖以提供解析解（如直线布置下直线→直线，无需采样）
    virtual ZcGeNurbCurve3d mapCurve(const ZcGeCurve3d& curve, int sampleCount = 32) const
    {
        std::vector<ZcGePoint3d> mappedPts;
        mappedPts.reserve(sampleCount + 1);
        ZcGeInterval interval;
        curve.getInterval(interval);
        double tStart = interval.lowerBound();
        double tEnd = interval.upperBound();
        double step = (tEnd - tStart) / sampleCount;
        for (int i = 0; i <= sampleCount; ++i) {
            mappedPts.push_back(mapPoint(curve.evalPoint(tStart + step * i)));
        }
        return fitNurbsCurve(mappedPts);
    }

    // ─── 面映射 ───
    // 默认：在 UV 方向各采样 M×N 个点 → mapPoint → 拟合 NurbSurface
    virtual ZcGeNurbSurface mapSurface(const ZcGeSurface& surface,
                                       int uCount = 16, int vCount = 16) const
    {
        std::vector<ZcGePoint3d> grid;
        grid.reserve((uCount + 1) * (vCount + 1));
        ZcGeInterval uRange, vRange;
        surface.getEnvelope(uRange, vRange);
        double uStep = (uRange.upperBound() - uRange.lowerBound()) / uCount;
        double vStep = (vRange.upperBound() - vRange.lowerBound()) / vCount;
        for (int i = 0; i <= uCount; ++i) {
            double u = uRange.lowerBound() + uStep * i;
            for (int j = 0; j <= vCount; ++j) {
                double v = vRange.lowerBound() + vStep * j;
                grid.push_back(mapPoint(surface.evalPoint(ZcGePoint2d(u, v))));
            }
        }
        return fitNurbsSurface(grid, uCount + 1, vCount + 1);
    }

    // ─── 体映射（Solid / BRep）───
    // 对体的每个面分别调用 mapSurface，重新缝合为 BRep
    virtual std::unique_ptr<ZcBrBrep> mapSolid(const ZcBrBrep& solid) const
    {
        // 遍历 solid 所有 Face
        //   对每个 Face 获取底层 Surface → mapSurface
        //   对每条 Edge 获取底层 Curve → mapCurve
        //   用映射后的 Face/Edge 重新构建 BRep
        return rebuildBrep(solid);
    }

private:
    static ZcGeNurbCurve3d fitNurbsCurve(const std::vector<ZcGePoint3d>& pts);
    static ZcGeNurbSurface fitNurbsSurface(const std::vector<ZcGePoint3d>& grid,
                                           int rows, int cols);
    static std::unique_ptr<ZcBrBrep> rebuildBrep(const ZcBrBrep& solid);
};

class LineCompStrategy final : public ILinearCompStrategy {
public:
    explicit LineCompStrategy(std::shared_ptr<const LineCompGeometry> geo)
        : m_geo(std::move(geo)) {}

    ZcGePoint3d mapPoint(const ZcGePoint3d& pt) const override
    {
        ZcGeVector3d dir = m_geo->end - m_geo->start;
        if (dir.length() < 1e-10)
            return m_geo->start;

        ZcGeVector3d xDir = dir.normal();

        ZcGeVector3d zRef(0.0, 0.0, 1.0);
        if (xDir.isParallelTo(zRef))
            zRef.set(0.0, 1.0, 0.0);

        ZcGeVector3d yDir = zRef.crossProduct(xDir).normal();
        ZcGeVector3d zDir = xDir.crossProduct(yDir).normal();

        return m_geo->start + xDir * pt.x + yDir * pt.y + zDir * pt.z;
    }

    // 直线布置下，直线仍映射为直线 → 解析解，无需采样
    ZcGeNurbCurve3d mapCurve(const ZcGeCurve3d& curve, int) const override
    {
        if (curve.isKindOf(ZcGe::kLineSeg3d)) {
            const auto& line = static_cast<const ZcGeLineSeg3d&>(curve);
            ZcGePoint3d p0 = mapPoint(line.startPoint());
            ZcGePoint3d p1 = mapPoint(line.endPoint());
            // 通过 Geometry::toGeCurve() 模式构造，保持与数据源的一致性
            LineCompGeometry mapped;
            mapped.start = p0;
            mapped.end = p1;
            return ZcGeNurbCurve3d(mapped.toGeCurve());
        }
        return ILinearCompStrategy::mapCurve(curve);  // fallback 到采样
    }

    // 获取定位线的 Ge 曲线表示（便于 Strategy 内部几何查询）
    ZcGeLineSeg3d pathCurve() const { return m_geo->toGeCurve(); }

private:
    std::shared_ptr<const LineCompGeometry> m_geo;
};

class ArcCompStrategy final : public ILinearCompStrategy {
public:
    explicit ArcCompStrategy(std::shared_ptr<const ArcCompGeometry> geo)
        : m_geo(std::move(geo)) {}

    ZcGePoint3d mapPoint(const ZcGePoint3d& pt) const override
    {
        double r = m_geo->radius();
        if (r < 1e-10)
            return m_geo->center;

        ZcGeVector3d radialStart = (m_geo->start - m_geo->center).normal();
        const ZcGeVector3d& arcNormal = m_geo->normal;

        double theta = pt.x / r;

        // Rodrigues 旋转
        ZcGeVector3d radial = radialStart * std::cos(theta)
            + arcNormal.crossProduct(radialStart) * std::sin(theta);

        return m_geo->center + radial * (r + pt.y) + arcNormal * pt.z;
    }

    // 获取定位线的 Ge 曲线表示（便于 Strategy 内部几何查询）
    ZcGeCircArc3d pathCurve() const { return m_geo->toGeCurve(); }

private:
    std::shared_ptr<const ArcCompGeometry> m_geo;
};
```

#### 2.1.2 样板几何映射策略

**映射接口分层**：`ILinearCompStrategy` 提供四个层次的映射方法，从基础到高阶依次构建：

```
mapPoint (纯虚)          ← 所有映射的基础原语
  ↑ 被调用
mapCurve (虚 + 默认)     ← 曲线：采样 → mapPoint → 拟合 NurbCurve
  ↑ 被调用
mapSurface (虚 + 默认)   ← 面：UV 网格采样 → mapPoint → 拟合 NurbSurface
  ↑ 被调用
mapSolid (虚 + 默认)     ← 体：遍历 Face/Edge → mapSurface/mapCurve → 重建 BRep
```

**曲线映射结果**：

| 样板源类型 | 直线布置后 | 圆弧布置后 | 是否有解析解 |
|-----------|-----------|-----------|-------------|
| 直线（沿 X） | 直线 | 圆弧 | 直线布置：`LineCompStrategy::mapCurve` 解析解 |
| 直线（沿 Y/Z） | 直线 | 直线 | 两者都可解析 |
| 圆弧 | 圆弧 | 一般曲线 | 圆弧布置走 fallback 采样 |
| 样条曲线 | 一般曲线 | 一般曲线 | 两者都走 fallback 采样 |
| 面 | 面 | 扭曲面 | 多数走 fallback UV 网格采样 |
| 体 | 体 | 扭曲体 | 逐面映射 → 重建 BRep |

**默认 fallback 机制**：

- **曲线**：基类 `mapCurve` 等参采样 N 点 → `mapPoint` → 拟合 `ZcGeNurbCurve3d`。
- **面**：基类 `mapSurface` UV 网格采样 M×N 点 → `mapPoint` → 拟合 `ZcGeNurbSurface`。
- **体**：基类 `mapSolid` 遍历所有 Face/Edge → 分别调用 `mapSurface`/`mapCurve` → 缝合重建 BRep。
- 子类可覆盖任一层提供解析解（如 `LineCompStrategy::mapCurve` 对直线源直接两点映射）。

**采样密度**：由曲线长度与曲率自适应决定。Jig 预览时可降低采样数以保证帧率，commit 时使用完整精度。

### 2.2 自定义实体

#### 2.2.1 继承方案：实体派生

```
ZcDbBmInsertableElement
  └── ZcDbBmLinearElement         （线性构件基类：公共数据 + 缓存 + 绘制框架）
        ├── ZcDbBmLineElement     （直线构件：持有 LineCompGeometry，独立序列化/夹点/变换）
        └── ZcDbBmArcElement      （圆弧构件：持有 ArcCompGeometry，独立序列化/夹点/变换）
```

**选择实体派生的理由**：

- `dwgInFields/dwgOutFields`、`subGetGripPoints`、`subMoveGripPointsAt`、`subTransformBy` 等 `sub*` 虚函数是 ZRX 框架为实体继承设计的差异化扩展点，由实体子类 override 是**最自然的归属**。
- 直线构件与圆弧构件的数据成员**确实不同**（`LineCompGeometry` vs `ArcCompGeometry`），各子类直接持有具体类型，无需多态间接和向下转型。
- Geometry 保持为纯数据结构，不承担序列化/夹点/变换等 UI 层职责，符合单一职责原则。
- 映射算法的差异仍由 `ILinearCompStrategy` 策略模式隔离，实体派生与策略模式各司其职，没有职责重叠。

#### 2.2.2 映射后的绘制单元

```cpp
struct MappedPrimitive {
    enum class Kind : uint8_t { kLine, kArc, kNurbs, kMesh };
    Kind kind;
    std::vector<ZcGePoint3d> points;
    ZcCmColor color;
    ZcDbObjectId layerId;
    ZcDbObjectId linetypeId;
};
```

#### 2.2.3 基类 `ZcDbBmLinearElement`

基类承担所有子类共享的职责：族类型引用、绘制缓存框架、`rebuildCache` 流程。
差异化的序列化/夹点/变换/深拷贝留给子类 override。

```cpp
class ZcDbBmLinearElement : public ZcDbBmInsertableElement {
    ACRX_DECLARE_MEMBERS(ZcDbBmLinearElement);

public:
    virtual LinearCompType linearCompType() const = 0;
    virtual double pathLength() const = 0;

    // ─── 映射校验状态查询（转发到 Impl 层，见 2.3.4） ───
    EMappingValidity mappingValidity() const;
    EMappingSubCode  mappingSubCode() const;
    bool isMappingValid() const;

    // ─── 绘制（基类统一实现）───
    Adesk::Boolean subWorldDraw(ZcGiWorldDraw* pWd) override
    {
        if (!m_cacheValid) {
            rebuildCache();
            m_cacheValid = true;
        }
        for (const auto& prim : m_cachedPrimitives) {
            drawPrimitive(pWd, prim);
        }
        return Adesk::kTrue;
    }

protected:
    ZcDbSoftPointerId m_familyTypeId;

    mutable bool m_cacheValid = false;
    mutable std::vector<MappedPrimitive> m_cachedPrimitives;

    void invalidateCache() { m_cacheValid = false; }

    // 子类提供具体几何 → 基类负责映射调度
    virtual std::unique_ptr<ILinearCompStrategy> createStrategy() const = 0;

    void rebuildCache()
    {
        m_cachedPrimitives.clear();
        auto strategy = createStrategy();
        if (!strategy) return;
        // 1. 打开 m_familyTypeId 指向的族类型
        // 2. 遍历族类型模型空间中的所有参与映射实体
        // 3. 对每个实体执行 strategy->mapPoint / mapSampledCurve
        // 4. 生成 MappedPrimitive 列表（携带图层/颜色/线型属性）
    }

    // 序列化基类公共字段
    Zcad::ErrorStatus dwgOutFieldsBase(ZcDbDwgFiler* pFiler) const
    {
        auto es = ZcDbBmInsertableElement::dwgOutFields(pFiler);
        if (es != Zcad::eOk) return es;
        pFiler->writeSoftPointerId(m_familyTypeId);
        return Zcad::eOk;
    }

    Zcad::ErrorStatus dwgInFieldsBase(ZcDbDwgFiler* pFiler)
    {
        auto es = ZcDbBmInsertableElement::dwgInFields(pFiler);
        if (es != Zcad::eOk) return es;
        pFiler->readSoftPointerId(&m_familyTypeId);
        return Zcad::eOk;
    }
};
```

**映射校验查询接口实现**（API 层转发到 Impl 层）：

```cpp
EMappingValidity ZcDbBmLinearElement::mappingValidity() const {
    return static_cast<const ZcDbBmLinearElementImpl*>(getImp())->mappingValidity();
}

EMappingSubCode ZcDbBmLinearElement::mappingSubCode() const {
    return static_cast<const ZcDbBmLinearElementImpl*>(getImp())->mappingSubCode();
}

bool ZcDbBmLinearElement::isMappingValid() const {
    return static_cast<const ZcDbBmLinearElementImpl*>(getImp())->isMappingValid();
}
```

#### 2.2.4 子类 `ZcDbBmLineElement`（直线构件）

```cpp
class ZcDbBmLineElement final : public ZcDbBmLinearElement {
    ACRX_DECLARE_MEMBERS(ZcDbBmLineElement);

public:
    LinearCompType linearCompType() const override { return LinearCompType::kLine; }
    double pathLength() const override { return m_geo.pathLength(); }

    const LineCompGeometry& geometry() const { return m_geo; }
    void setGeometry(const LineCompGeometry& geo) { m_geo = geo; invalidateCache(); }

    // ─── 序列化 ───
    Zcad::ErrorStatus dwgOutFields(ZcDbDwgFiler* pFiler) const override
    {
        assertReadEnabled();
        auto es = dwgOutFieldsBase(pFiler);
        if (es != Zcad::eOk) return es;
        pFiler->writePoint3d(m_geo.start);
        pFiler->writePoint3d(m_geo.end);
        return Zcad::eOk;
    }

    Zcad::ErrorStatus dwgInFields(ZcDbDwgFiler* pFiler) override
    {
        assertWriteEnabled();
        auto es = dwgInFieldsBase(pFiler);
        if (es != Zcad::eOk) return es;
        pFiler->readPoint3d(&m_geo.start);
        pFiler->readPoint3d(&m_geo.end);
        invalidateCache();
        return Zcad::eOk;
    }

    // ─── 夹点 ───
    Zcad::ErrorStatus subGetGripPoints(
        ZcGePoint3dArray& gripPoints,
        ZcDbIntArray& osnapModes,
        ZcDbIntArray& geomIds) const override
    {
        assertReadEnabled();
        gripPoints.append(m_geo.start);   // grip 0: 起点
        gripPoints.append(m_geo.end);     // grip 1: 终点
        return Zcad::eOk;
    }

    Zcad::ErrorStatus subMoveGripPointsAt(
        const ZcDbIntArray& indices,
        const ZcGeVector3d& offset) override
    {
        assertWriteEnabled();
        LineCompGeometry snapshot = m_geo;   // Memento: 值拷贝快照

        for (int i = 0; i < indices.length(); ++i) {
            switch (indices[i]) {
            case 0: m_geo.start += offset; break;
            case 1: m_geo.end   += offset; break;
            }
        }

        // TODO: 校验 → 失败则 m_geo = snapshot; return eInvalidInput;
        invalidateCache();
        return Zcad::eOk;
    }

    // ─── 通用变换 ───
    Zcad::ErrorStatus subTransformBy(const ZcGeMatrix3d& xform) override
    {
        assertWriteEnabled();
        m_geo.start.transformBy(xform);
        m_geo.end.transformBy(xform);
        invalidateCache();
        return Zcad::eOk;
    }

protected:
    std::unique_ptr<ILinearCompStrategy> createStrategy() const override
    {
        auto geo = std::make_shared<LineCompGeometry>(m_geo);
        return std::make_unique<LineCompStrategy>(std::move(geo));
    }

private:
    LineCompGeometry m_geo;
};
```

#### 2.2.5 子类 `ZcDbBmArcElement`（圆弧构件）

```cpp
class ZcDbBmArcElement final : public ZcDbBmLinearElement {
    ACRX_DECLARE_MEMBERS(ZcDbBmArcElement);

public:
    LinearCompType linearCompType() const override { return LinearCompType::kArc; }
    double pathLength() const override { return m_geo.pathLength(); }

    const ArcCompGeometry& geometry() const { return m_geo; }
    void setGeometry(const ArcCompGeometry& geo) { m_geo = geo; invalidateCache(); }

    // ─── 序列化 ───
    Zcad::ErrorStatus dwgOutFields(ZcDbDwgFiler* pFiler) const override
    {
        assertReadEnabled();
        auto es = dwgOutFieldsBase(pFiler);
        if (es != Zcad::eOk) return es;
        pFiler->writePoint3d(m_geo.start);
        pFiler->writePoint3d(m_geo.end);
        pFiler->writePoint3d(m_geo.center);
        pFiler->writeVector3d(m_geo.normal);
        return Zcad::eOk;
    }

    Zcad::ErrorStatus dwgInFields(ZcDbDwgFiler* pFiler) override
    {
        assertWriteEnabled();
        auto es = dwgInFieldsBase(pFiler);
        if (es != Zcad::eOk) return es;
        pFiler->readPoint3d(&m_geo.start);
        pFiler->readPoint3d(&m_geo.end);
        pFiler->readPoint3d(&m_geo.center);
        pFiler->readVector3d(&m_geo.normal);
        invalidateCache();
        return Zcad::eOk;
    }

    // ─── 夹点 ───
    Zcad::ErrorStatus subGetGripPoints(
        ZcGePoint3dArray& gripPoints,
        ZcDbIntArray& osnapModes,
        ZcDbIntArray& geomIds) const override
    {
        assertReadEnabled();
        gripPoints.append(m_geo.start);    // grip 0: 起点
        gripPoints.append(m_geo.end);      // grip 1: 终点
        gripPoints.append(m_geo.center);   // grip 2: 圆心
        return Zcad::eOk;
    }

    Zcad::ErrorStatus subMoveGripPointsAt(
        const ZcDbIntArray& indices,
        const ZcGeVector3d& offset) override
    {
        assertWriteEnabled();
        ArcCompGeometry snapshot = m_geo;   // Memento: 值拷贝快照

        for (int i = 0; i < indices.length(); ++i) {
            switch (indices[i]) {
            case 0: m_geo.start  += offset; break;
            case 1: m_geo.end    += offset; break;
            case 2: m_geo.center += offset; break;
            }
        }

        // TODO: 校验 → 失败则 m_geo = snapshot; return eInvalidInput;
        invalidateCache();
        return Zcad::eOk;
    }

    // ─── 通用变换 ───
    Zcad::ErrorStatus subTransformBy(const ZcGeMatrix3d& xform) override
    {
        assertWriteEnabled();
        m_geo.start.transformBy(xform);
        m_geo.end.transformBy(xform);
        m_geo.center.transformBy(xform);
        m_geo.normal.transformBy(xform);
        invalidateCache();
        return Zcad::eOk;
    }

protected:
    std::unique_ptr<ILinearCompStrategy> createStrategy() const override
    {
        auto geo = std::make_shared<ArcCompGeometry>(m_geo);
        return std::make_unique<ArcCompStrategy>(std::move(geo));
    }

private:
    ArcCompGeometry m_geo;
};
```

#### 2.2.6 缓存刷新策略

| 触发时机 | 动作 |
|---------|------|
| `subMoveGripPointsAt` | 子类修改 `m_geo` → 调用基类 `invalidateCache()` |
| `subTransformBy` | 子类变换 `m_geo` → 调用基类 `invalidateCache()` |
| `dwgInFields` | 子类反序列化后 → 调用基类 `invalidateCache()` |
| `族类型修改通知` | Reactor 回调 → 调用基类 `invalidateCache()` |
| `subWorldDraw` | 若 `!m_cacheValid` → 基类 `rebuildCache()` → 子类 `createStrategy()` |

`rebuildCache()` 在基类中实现，通过子类 override 的 `createStrategy()` 获取对应的映射策略。

### 2.3 映射校验与分层响应

> **改造说明**：原方案中校验框架（`ValidationPipe` + `IValidationRule`）设计为在 `recomputeModelBlock` 外部独立调用。但内网实际实现中，映射过程本身嵌在 `recomputeModelBlock` 内部（`mapInstanceModelBlk` / `mapUnBlockEnts`），映射失败即等价于重量级校验失败。本节将校验与映射统一，通过状态成员变量桥接 `recomputeModelBlock` 内部与上层（Jig / 夹点 / commit）的分层响应。

#### 2.3.1 设计原则

- **校验不独立于映射**：轻量预检在映射前执行，重量级校验由映射过程本身承担（映射失败 = 校验失败）。
- **`Zcad::ErrorStatus` 只表示系统错误**：IO 失败、对象打开失败等。映射合法性通过成员变量 `m_mappingValidity` 传递，不再复用 `eNotApplicable` 表示业务非法。
- **状态成员不序列化**：`m_mappingValidity` 等是运行时瞬态，文件加载后首次绘制触发 `recomputeModelBlock` 自动重算，序列化无意义且增加版本兼容负担。
- **分层响应由上层决定**：`recomputeModelBlock` 只负责设置状态，弹窗 / 回退 / 不显示等行为由调用方（绘制层 / Jig / commit）根据状态决定。

#### 2.3.2 映射校验枚举

一级码 `EMappingValidity` 用于业务分类和流程控制，二级码 `EMappingSubCode` 用于定位具体失败原因（日志和 Error 对话框）：

```cpp
enum class EMappingValidity : uint8_t {
    kUnknown = 0,      // 尚未执行映射（初始状态 / 刚反序列化）
    kValid,            // 映射成功，几何合法
    kPathInvalid,      // 定位线本身非法（长度为零、无法定义有效圆弧等）
    kPathTooShort,     // 路径长度不足以容纳样板最小 X 范围
    kMapFailed         // 映射过程中检测到几何非法（自交、退化、缝合失败等）
};

enum class EMappingSubCode : uint16_t {
    kNone = 0,
    // 路径级
    kLineTooShort,         // 直线长度 < 系统最小有效长度
    kArcInvalid,           // 三点无法定义有效圆弧
    kPathLengthNotEnough,  // 路径长度 < 样板最小 X 范围
    // 映射级
    kCurveMapFailed,       // 曲线映射后非法（交叉、回头、折返）
    kSurfaceMapFailed,     // 面映射后非法
    kSolidMapFailed,       // 体映射后非法
    kSelfIntersection,     // 映射后轮廓自交
    kOpenLoop,             // 闭合轮廓映射后无法形成有效闭合区域
    kNonManifold,          // 内外环关系错误
    kDegenerateFace        // 映射后边界退化为零长度边 / 极短边
};
```

#### 2.3.3 样板最小范围求解

- 以样板局部坐标系 X 方向作为最小长度约束方向。
- 对样板全部参与映射对象做投影，合并为全局区间 `[xmin, xmax]`。
- `TemplateMinXRange = (xmax - xmin) + 2 * kModelTol`
- 此结果在样板加载或参数变化后重算并缓存，Jig 预览阶段只读取缓存。

**说明**："路径长度不足以容纳样板最小实体"的含义：样板中所有参与映射的实体在 X 方向上的投影区间为 `[xmin, xmax]`，这个区间的长度就是该样板的"最小路径长度需求"。当用户绘制的定位线（直线长度 / 圆弧弧长）小于此值时，映射后的实体会发生压缩、重叠或超出边界，属于非法状态。

#### 2.3.4 `ZcDbBmLinearElementImpl` 新增成员

```cpp
class ZcDbBmLinearElementImpl : public ZcDbBmTemplateInstanceImp {
    // ... 现有成员 ...

protected:
    // ── 映射校验状态（运行时瞬态，不参与 dwgIn/dwgOut） ──
    EMappingValidity m_mappingValidity    = EMappingValidity::kUnknown;
    EMappingSubCode  m_mappingSubCode     = EMappingSubCode::kNone;
    ZcString         m_mappingErrorDetail;

    // ── 递归防御标志（不参与 dwgIn/dwgOut） ──
    bool m_bInRollbackRecompute = false;

public:
    // ── 查询接口 ──
    EMappingValidity mappingValidity() const { return m_mappingValidity; }
    EMappingSubCode  mappingSubCode()  const { return m_mappingSubCode; }
    const ZcString&  mappingErrorDetail() const { return m_mappingErrorDetail; }

    bool isMappingValid() const {
        return m_mappingValidity == EMappingValidity::kValid;
    }

    void resetMappingValidity() {
        m_mappingValidity    = EMappingValidity::kUnknown;
        m_mappingSubCode     = EMappingSubCode::kNone;
        m_mappingErrorDetail.setEmpty();
    }

protected:
    // ── 内部辅助：统一设置映射状态 ──
    void setMappingValid() {
        m_mappingValidity    = EMappingValidity::kValid;
        m_mappingSubCode     = EMappingSubCode::kNone;
        m_mappingErrorDetail.setEmpty();
    }

    void setMappingFailed(EMappingValidity validity,
                          EMappingSubCode  subCode,
                          const ZcString&  detail) {
        m_mappingValidity    = validity;
        m_mappingSubCode     = subCode;
        m_mappingErrorDetail = detail;
    }
};
```

**不序列化的理由**：

| 成员 | 为什么不 dwgIn/dwgOut |
|------|----------------------|
| `m_mappingValidity` | 运行时瞬态，由每次 `recomputeModelBlock` 重算得到。文件加载后第一次绘制即触发 recompute，自动恢复正确状态 |
| `m_mappingSubCode` | 同上，是 `m_mappingValidity` 的细化信息 |
| `m_mappingErrorDetail` | 同上，是人可读的错误描述 |
| `m_bInRollbackRecompute` | 纯防御性标志，仅在单次 recompute 调用栈内有效 |

#### 2.3.5 轻量预检函数

在 `recomputeModelBlock` 映射前执行，检查定位线本身是否合法。规则数量少且固定，直接函数调用即可（不需要 `ValidationPipe` 框架）：

```cpp
EMappingValidity ZcDbBmLinearElementImpl::precheckLocateInfo() {
    const IZcBmLinearLocateInfo* info = getLocateInfoPtr();
    if (!info) {
        setMappingFailed(EMappingValidity::kPathInvalid,
                         EMappingSubCode::kNone,
                         L"定位线数据为空");
        return m_mappingValidity;
    }

    double len = info->length();

    // 规则 1: 路径非退化
    if (len < kMinPathLength) {
        EMappingSubCode sub = EMappingSubCode::kLineTooShort;
        // 如果是圆弧类型，使用更具体的子码
        if (dynamic_cast<const ZcBmArcLocateInfo*>(info))
            sub = EMappingSubCode::kArcInvalid;
        setMappingFailed(EMappingValidity::kPathInvalid, sub, L"定位线无效");
        return m_mappingValidity;
    }

    // 规则 2: 路径长度 >= 样板最小 X 范围
    double templateMinX = getTemplateMinXRange();
    if (templateMinX > 0.0 && len < templateMinX) {
        ZcString detail;
        detail.format(L"路径长度 %.2f 不足以容纳样板最小范围 %.2f",
                      len, templateMinX);
        setMappingFailed(EMappingValidity::kPathTooShort,
                         EMappingSubCode::kPathLengthNotEnough,
                         detail);
        return m_mappingValidity;
    }

    // 预检通过，暂不写 kValid（等映射完成后再写）
    return EMappingValidity::kValid;
}
```

#### 2.3.6 `recomputeModelBlock` 改造

完整改造后的核心函数，包含递归防御、轻量预检、映射执行、状态设置、分支处理。

首先提取重复的回退逻辑为私有方法：

```cpp
// 映射非法时的统一回退入口：rollback 恢复 snapshot 并递归重算
// 返回 true 表示已执行回退并重算完成（调用方应直接 return es）
// 返回 false 表示已处于回退重算中（防御性路径，调用方应返回 eOk）
bool ZcDbBmLinearElementImpl::tryRollbackAndRecompute(
    bool bForceUpdate, bool bKeepNestProperty, Zcad::ErrorStatus& outEs)
{
    if (m_bInRollbackRecompute) {
        // 已在回退重算中（snapshot 也失败），保持当前失败状态
        outEs = Zcad::eOk;
        return false;
    }

    m_bInRollbackRecompute = true;
    rollback();
    outEs = recomputeModelBlock(bForceUpdate, bKeepNestProperty);
    m_bInRollbackRecompute = false;
    return true;
}
```

然后 `recomputeModelBlock` 主体：

```cpp
Zcad::ErrorStatus ZcDbBmLinearElementImpl::recomputeModelBlock(
    bool bForceUpdate, bool bKeepNestProperty)
{
    // ── Step 1: 判断是否需要映射 ──
    ZBOOL bNeedMap = FALSE;
    for (auto& iter : m_ClassInstanceContainer) {
        if (iter->isDataChanged()) {
            bNeedMap = TRUE;
        }
    }

    // ── Step 2: 基类参数化计算（属性差异比对、ModelBlock 实体拷贝/复用） ──
    Zcad::ErrorStatus es =
        ZcDbBmTemplateInstanceImp::recomputeModelBlock(bForceUpdate, bKeepNestProperty);
    if (Zcad::eOk != es)
        return es;

    // ── Step 3: 分支 — 已入库实体 vs Jig 临时实体 ──
    if (getApiDbObject()->objectId().isValid()) {
        // ════════════════════════════════════════
        // 已入库实体（subClose / 夹点拖动触发）
        // ════════════════════════════════════════
        if (!bNeedMap)
            return Zcad::eOk;

        ZcDbObjectId modelBlockId = getModelBlock();
        if (!modelBlockId.isValid()) {
            // TODO: 线性构件始终需要独立实例 ModelBlock
            // 从族类型 ModelBlock 深拷贝一份
        }

        // ── 轻量预检 ──
        if (precheckLocateInfo() != EMappingValidity::kValid) {
            tryRollbackAndRecompute(bForceUpdate, bKeepNestProperty, es);
            return es;
        }

        // ── 映射（mapInstanceModelBlk 内部写 m_mappingValidity） ──
        es = mapInstanceModelBlk(modelBlockId);
        if (es != Zcad::eOk)
            return es;  // 系统级错误（IO/对象打开失败）

        if (m_mappingValidity != EMappingValidity::kValid) {
            tryRollbackAndRecompute(bForceUpdate, bKeepNestProperty, es);
            return es;
        }

    } else {
        // ════════════════════════════════════════
        // Jig 临时实体（subViewportDraw 触发）
        // ════════════════════════════════════════
        if (precheckLocateInfo() != EMappingValidity::kValid) {
            // 状态已写入成员，绘制层读取后决定不显示
            return Zcad::eOk;
        }

        es = mapUnBlockEnts((ZcDbBmTemplateInstance*)getBmApiObject());
        if (es != Zcad::eOk)
            return es;  // 系统级错误

        // m_mappingValidity 已在 mapUnBlockEnts 内部设置
    }

    return Zcad::eOk;
}
```

**`tryRollbackAndRecompute` 封装说明**：

预检失败和映射失败后的回退逻辑完全一致（rollback → 递归 recompute），提取为单独方法消除重复。调用方只需一行：

```cpp
if (/* 非法 */) {
    tryRollbackAndRecompute(bForceUpdate, bKeepNestProperty, es);
    return es;
}
```

**递归防御机制**：

```
recomputeModelBlock()
  → 预检或映射检测到非法
  → tryRollbackAndRecompute()
    → m_bInRollbackRecompute == false（正常路径）
      → m_bInRollbackRecompute = true
      → rollback()            ← setLocateInfo(m_snapshot) 恢复上次合法数据
      → recomputeModelBlock() ← 用旧数据重算（最多一层递归）
        → 旧数据通过 → kValid → 正常返回
        → 旧数据也失败 → tryRollbackAndRecompute()
          → m_bInRollbackRecompute == true（防御路径）
          → 不递归，outEs = eOk，保持失败状态直接返回
      → m_bInRollbackRecompute = false
```

`rollback()` 恢复的是上次验证合法的 `m_snapshot`，正常情况下用旧数据重算一定成功，因此最多递归一层。`m_bInRollbackRecompute` 标志是纯防御性编程，防止极端场景下的无限递归。

#### 2.3.7 `mapInstanceModelBlk` 改造

返回值 `Zcad::ErrorStatus` 回归本职：仅表示系统级错误。映射合法性通过直接写成员变量 `m_mappingValidity` 传递：

```cpp
Zcad::ErrorStatus ZcDbBmLinearElementImpl::mapInstanceModelBlk(
    const ZcDbObjectId& mbId)
{
    if (!mbId.isValid())
        return Zcad::eNullObjectId;

    ZcDbBlockTableRecord* pModelBlock = nullptr;
    Zcad::ErrorStatus es = zcdbOpenObject(pModelBlock, mbId, ZcDb::kForWrite);
    if (Zcad::eOk != es)
        return es;

    ZcDbBlockTableRecordIterator* pItr = nullptr;
    pModelBlock->newIterator(pItr);
    pModelBlock->close();
    if (!pItr)
        return Zcad::eNullIterator;

    std::unique_ptr<IZcBmLinearMapAlgo> pMapAlgo = this->createMapAlgo();
    bool bMappingFailed = false;

    for (pItr->start(); !pItr->done(); pItr->step()) {
        ZcDbEntity* pEnt = nullptr;
        pItr->getEntity(pEnt, ZcDb::kForWrite);
        if (!pEnt) continue;

        // 跳过基准实体
        if (pEnt->isKindOf(ZcDbBmDatumBase::desc()) ||
            pEnt->isKindOf(ZcDbBmDatumCoordinate::desc())) {
            pEnt->close();
            continue;
        }

        // 嵌套 TemplateInstance 处理
        if (pEnt->isKindOf(ZcDbBmTemplateInstance::desc())) {
            ZcDbBmTemplateInstance* pTmpInstance =
                ZcDbBmTemplateInstance::cast(pEnt);
            ZcDbObjectId curMBId = pTmpInstance->getImp()->getModelBlock();

            es = mapInstanceModelBlk(curMBId);
            if (es != Zcad::eOk) {
                pEnt->close();
                break;
            }
            if (m_mappingValidity != EMappingValidity::kValid) {
                bMappingFailed = true;
                pEnt->close();
                break;
            }
        }

        // 曲线映射
        if (pEnt->isKindOf(ZcDbCurve::desc())) {
            ZcDbCurve* pCurve = ZcDbCurve::cast(pEnt);
            ZcDbCurve* pNewCurve = nullptr;
            Zcad::ErrorStatus mapEs = pMapAlgo->mapCurve(pCurve, pNewCurve);

            // mapCurve 返回值仍用 eNotApplicable 表示"无法映射"
            // 这里将其转译为业务语义
            if (mapEs != Zcad::eOk && mapEs != Zcad::eNotApplicable) {
                pEnt->close();
                delete pItr;
                return mapEs;  // 真正的系统错误
            }

            if (mapEs == Zcad::eNotApplicable) {
                setMappingFailed(EMappingValidity::kMapFailed,
                                 EMappingSubCode::kCurveMapFailed,
                                 L"曲线映射后几何非法");
                bMappingFailed = true;
                pEnt->close();
                break;
            }

            // 映射成功：用新曲线替换旧曲线
            if (pNewCurve) {
                es = pCurve->handOverTo(pNewCurve);
                if (es == Zcad::eObjectToBeDeleted) {
                    pNewCurve->close();
                    delete pCurve;
                } else {
                    delete pNewCurve;
                    pEnt->close();
                }
                continue;
            }
        }

        // 面映射
        if (pEnt->isKindOf(ZcDbSurface::desc())) {
            Zcad::ErrorStatus mapEs = pMapAlgo->mapSurface(
                ZcDbSurface::cast(pEnt));
            if (mapEs == Zcad::eNotApplicable) {
                setMappingFailed(EMappingValidity::kMapFailed,
                                 EMappingSubCode::kSurfaceMapFailed,
                                 L"面映射后几何非法");
                bMappingFailed = true;
                pEnt->close();
                break;
            }
        }

        // 体映射
        if (pEnt->isKindOf(ZcDbBmFeatureSolid::desc())) {
            Zcad::ErrorStatus mapEs = pMapAlgo->mapSolid(
                ZcDbBmFeatureSolid::cast(pEnt));
            if (mapEs == Zcad::eNotApplicable) {
                setMappingFailed(EMappingValidity::kMapFailed,
                                 EMappingSubCode::kSolidMapFailed,
                                 L"体映射后几何非法");
                bMappingFailed = true;
                pEnt->close();
                break;
            }
        }

        pEnt->close();
    }

    delete pItr;
    pItr = nullptr;

    if (!bMappingFailed) {
        setMappingValid();
    }
    // bMappingFailed == true 时，m_mappingValidity 已在上方 setMappingFailed 中设置

    return Zcad::eOk;  // 系统操作成功（不论映射是否合法）
}
```

**关键语义变化**：`mapInstanceModelBlk` 的返回值 `Zcad::eOk` 仅表示"函数执行完成，没有系统级错误"，**不代表映射合法**。调用方必须检查 `m_mappingValidity` 判断映射结果。底层 `mapCurve` / `mapSurface` / `mapSolid` 返回的 `eNotApplicable` 在此处被转译为 `EMappingValidity::kMapFailed` + 具体 `EMappingSubCode`，不再向上透传。

#### 2.3.8 `mapUnBlockEnts` 改造

Jig 临时实体场景，对未入库的 UnBlockEnts 执行映射。语义与 `mapInstanceModelBlk` 一致：

```cpp
Zcad::ErrorStatus ZcDbBmLinearElementImpl::mapUnBlockEnts(
    ZcDbBmTemplateInstance* pInstance)
{
    std::unique_ptr<IZcBmLinearMapAlgo> pMapAlgo = this->createMapAlgo();

    ZcArray<ZcDbEntity*>* pUnBlockEnts = nullptr;
    if (pInstance == getBmApiObject()) {
        pUnBlockEnts = m_pUnBlockEnts;
    } else {
        auto* pImpl = pInstance->getImp();
        ZcDbBmInstanceElementBaseImp* pInstanceImp =
            dynamic_cast<ZcDbBmInstanceElementBaseImp*>(pImpl);
        if (!pInstanceImp)
            return Zcad::eNullPtr;
        pUnBlockEnts = pInstanceImp->getUnBlockEnts();
    }

    if (!pUnBlockEnts)
        return Zcad::eOk;

    for (int i = 0; i < pUnBlockEnts->length(); ++i) {
        ZcDbEntity* pEnt = pUnBlockEnts->at(i);
        if (!pEnt) continue;

        // 跳过基准实体
        if (pEnt->isKindOf(ZcDbBmDatumBase::desc()) ||
            pEnt->isKindOf(ZcDbBmDatumCoordinate::desc()))
            continue;

        // 嵌套 TemplateInstance
        if (pEnt->isKindOf(ZcDbBmTemplateInstance::desc())) {
            ZcDbBmTemplateInstance* pTmpInstance =
                ZcDbBmTemplateInstance::cast(pEnt);
            Zcad::ErrorStatus es = mapUnBlockEnts(pTmpInstance);
            if (es != Zcad::eOk)
                return es;
            if (m_mappingValidity != EMappingValidity::kValid &&
                m_mappingValidity != EMappingValidity::kUnknown) {
                return Zcad::eOk;  // 嵌套映射失败，状态已设置
            }
        }

        // 曲线映射
        if (pEnt->isKindOf(ZcDbCurve::desc())) {
            ZcDbCurve* pCurve = ZcDbCurve::cast(pEnt);
            ZcDbCurve* pNewCurve = nullptr;
            Zcad::ErrorStatus mapEs = pMapAlgo->mapCurve(pCurve, pNewCurve);

            if (mapEs == Zcad::eNotApplicable) {
                pCurve->setVisibility(ZcDb::kInvisible);
                setMappingFailed(EMappingValidity::kMapFailed,
                                 EMappingSubCode::kCurveMapFailed,
                                 L"曲线映射后几何非法");
                return Zcad::eOk;
            }

            if (pNewCurve) {
                (*pUnBlockEnts)[i] = pNewCurve;
                delete pCurve;
            }
        }
    }

    setMappingValid();
    return Zcad::eOk;
}
```

#### 2.3.9 绘制层控制

基类 `subViewportDraw` 中，在 `recomputeModelBlock` 触发后、实际绘制前，插入映射合法性判断：

```cpp
void ZcDbBmInstanceElementBaseImp::subViewportDraw(ZcGiViewportDraw* vd) {
    if (!vd) return;

    ZcDbBmElemVisibilityUtils utils((ZcDbEntity*)getApiDbObject());
    if (utils.isBmVisibilityFilter())
        return;

    // 未入库实体（Jig 预览）：在此触发 recomputeModelBlock
    if (getApiDbObject() && !getApiDbObject()->objectId().isValid()) {
        static_cast<ZcDbBmInstanceElementBase*>(getBmApiObject())
            ->recomputeModelBlock(FALSE);
    }

    // ── 新增：线性构件映射非法时跳过绘制（所有实体不显示） ──
    auto* pLinearImpl = dynamic_cast<ZcDbBmLinearElementImpl*>(this);
    if (pLinearImpl && !pLinearImpl->isMappingValid()) {
        return;
    }

    // ... 正常绘制流程（pushModelTransform、遍历 UnBlockEnts 等） ...
}
```

同理，`subWorldDraw` 中如需类似控制也可加入（视基类实现而定）。

#### 2.3.10 上层场景分层响应

三个场景对映射非法的响应行为不同，但都通过查询同一个 `m_mappingValidity` 成员实现：

| 场景 | 触发路径 | 映射非法时的行为 | 实现位置 |
|------|---------|---------------|---------|
| **Jig 预览** | `subViewportDraw` → `recomputeModelBlock`（else 分支） | 所有实体不显示 | 绘制层 `isMappingValid()` 判断（2.3.9） |
| **夹点拖动** | `syncDataToProperty` → `recomputeModelBlock`（if 分支） | rollback 恢复旧数据 | `recomputeModelBlock` 内部自动处理（2.3.6） |
| **Jig 入库** | `commit()` 中查询 | 弹 Error 对话框，取消入库 | `commit()` 函数中（见下方） |

**Jig `commit()` 中的入库校验：**

```cpp
bool LineCompJig::commit() {
    // Jig 拖动期间 recomputeModelBlock 已在每帧执行，
    // 此时 m_pPreviewEnt 内部 m_mappingValidity 反映最终一帧的状态。
    // 但 drag() 返回 kNormal 时用户点击的瞬间位置可能与最后一帧不同，
    // 需要用最终数据重新触发一次 recompute 以确保状态准确。

    auto* pLinearImpl = getLinearImpl(m_pPreviewEnt);

    // 强制一次完整 recompute（用最终确认的数据）
    pLinearImpl->recomputeModelBlock(/*bForceUpdate=*/true, /*bKeepNestProperty=*/true);

    if (!pLinearImpl->isMappingValid()) {
        // 弹 Error 对话框
        ZcString msg;
        msg.format(L"无法生成\"%s\".\"%s\"",
                   getComponentName(), getTypeName());
        // 对话框样式：Error 图标 + "确定" 按钮
        zcBmShowErrorDialog(L"中望BIM", msg);

        // 不入库，返回 false → doIt 返回 true → Jig 循环继续
        return false;
    }

    // 入库
    ZcDbObjectId entId;
    Zcad::ErrorStatus es = appendToModelSpace(m_pPreviewEnt, entId);
    if (es != Zcad::eOk) {
        zcutPrintf(_T("\n入库失败: %s"), zcadErrorStatusText(es));
        return false;
    }
    m_pPreviewEnt->close();
    markPreviewEntityAppended();
    return true;
}
```

**夹点拖动：** 不需要任何额外代码。`recomputeModelBlock` 内部已自动处理：检测到映射非法 → `rollback()` 恢复 `m_snapshot` → 递归重算成功 → 显示回退后的合法状态。整个过程对 `moveGripPointsAt` 调用方完全透明。

#### 2.3.11 扩展策略

- **新增路径类型**（如 `kSpline`）：在派生类 `ZcDbBmSplinePlacementImpl` 的 `precheckLocateInfo()` 中 override 或在基类函数中新增分支。
- **新增映射失败场景**：扩展 `EMappingSubCode` 枚举，在 `mapInstanceModelBlk` / `mapUnBlockEnts` 中对应位置调用 `setMappingFailed`。
- **业务交互策略（弹窗/回退）**：始终放在外部调用层（`commit()` / 绘制层），不进入映射函数内部。

### 2.4 布置流程

前提：**自定义实体类已经实现**（显示、持久化、夹点编辑均可用）。  
本节按仓库 ZRX 接口约束定义：**命令层 + Jig 层 + 校验层 + 入库层**。

#### 2.4.1 ZRX API 约束

- 命令注册：`zcedRegCmds->addCommand(...)`。
- 点输入：`zcedGetPoint(...)`（首点、次点）。
- 动态预览：Jig 的 `drag()` 机制。
- Jig 基类：`ZcEdJig`。
- Jig 核心虚函数：
  - `ZcEdJig::DragStatus sampler()`
  - `ZSoft::Boolean update()`
  - `ZcDbEntity* entity() const`
- 关键返回值处理：
  - `kNormal`：确认输入，进入下一步/提交
  - `kNoChange`：鼠标未变化，跳过重算
  - `kCancel`：用户取消，退出命令
  - `kNull`：空输入（Enter），按退出处理
  - `kOther`：输入存在但不满足业务约束（继续交互）

#### 2.4.2 统一命令入口

命令层统一入口：按 `LinearCompType` 创建对应 Jig，对外流程完全一致。

```cpp
void CmdLinearCompByType(LinearCompType type, const LinearCompRuntimeContext& ctx) {
    while (true) {
        std::unique_ptr<LinearCompJigBase> jig = LinearCompJigFactory::Create(type, ctx);
        if (jig == nullptr) {
            zcutPrintf(_T("\n创建布置Jig失败。"));
            return;
        }

        const bool keepRunning = jig->doIt();

        if (!keepRunning) {
            return; // Esc / Enter / Space
        }
    }
}
```

可选：命令先用关键字确定类型，再调用统一入口：

```cpp
void CmdLinearCompPlace() {
    LinearCompType type{};
    if (!selectLinearCompTypeByKeyword(&type)) return;

    const LinearCompRuntimeContext ctx = buildLinearCompRuntimeContext();
    CmdLinearCompByType(type, ctx);
}
```

#### 2.4.3 Jig 统一 doIt 模板（流程一致）

```cpp
class LinearCompJigBase : public ZcEdJig {
public:
    explicit LinearCompJigBase(const LinearCompRuntimeContext& ctx);
    ~LinearCompJigBase() override;

    bool doIt();
    ZcDbEntity* entity() const override { return m_pPreviewEnt; }

protected:
    virtual bool acquireFixedPoints() = 0;
    virtual const ZTCHAR* dynamicPrompt() const = 0;
    virtual bool commit() = 0;
    void replacePreviewEntity(ZcDbEntity* pEntity);
    void destroyPreviewEntity();
    void markPreviewEntityAppended();

    LinearCompRuntimeContext m_ctx;
    ZcDbEntity* m_pPreviewEnt = nullptr;
};

LinearCompJigBase::~LinearCompJigBase() {
    destroyPreviewEntity();
}

void LinearCompJigBase::replacePreviewEntity(ZcDbEntity* pEntity) {
    if (m_pPreviewEnt == pEntity) return;
    destroyPreviewEntity();
    m_pPreviewEnt = pEntity;
}

void LinearCompJigBase::destroyPreviewEntity() {
    if (m_pPreviewEnt == nullptr) return;
    delete m_pPreviewEnt;
    m_pPreviewEnt = nullptr;
}

void LinearCompJigBase::markPreviewEntityAppended() {
    m_pPreviewEnt = nullptr;
}

bool LinearCompJigBase::doIt() {
    if (!acquireFixedPoints()) {
        return false;
    }

    setDispPrompt(dynamicPrompt());
    const DragStatus stat = drag();

    if (stat == kCancel || stat == kNull) return false;
    if (stat != kNormal) return true;

    if (!commit()) {
        return true;
    }
    return true;
}
```

内存约束（Jig 内自定义实体）：

- `m_pPreviewEnt` 使用裸指针，不使用智能指针（ZRX Jig 框架要求 `entity()` 返回裸指针，且 `drag()` 期间该对象的生命周期由 Jig 管理）。
- `replacePreviewEntity()` 负责接管新对象并释放旧对象，防止 update 重建时泄漏。
- 未入库对象由 `LinearCompJigBase::~LinearCompJigBase()` 统一释放。
- 入库成功后先 `close()`，再调用 `markPreviewEntityAppended()` 清空指针，避免重复释放。

#### 2.4.4 LineCompJig

```cpp
class LineCompJig final : public LinearCompJigBase {
public:
    explicit LineCompJig(const LinearCompRuntimeContext& ctx);
    ~LineCompJig() override;

    DragStatus sampler() override;
    ZSoft::Boolean update() override;

protected:
    bool acquireFixedPoints() override;
    const ZTCHAR* dynamicPrompt() const override { return _T("\n指定构件终点: "); }
    bool commit() override;

private:
    ZcGePoint3d m_start;
    ZcGePoint3d m_end;
    ZcGePoint3d m_prevEnd;  // 上一帧终点，用于 sampler 去重
};

bool LineCompJig::acquireFixedPoints() {
    return zcedGetPoint(nullptr, _T("\n指定构件起点: "), asDblArray(m_start)) == RTNORM;
}

DragStatus LineCompJig::sampler()
{
    ZcGePoint3d newEnd;
    DragStatus stat = acquirePoint(newEnd);
    if (stat != kNormal) return stat;

    if (newEnd.isEqualTo(m_prevEnd))
        return kNoChange;

    m_prevEnd = newEnd;
    m_end = newEnd;
    return kNormal;
}

ZSoft::Boolean LineCompJig::update()
{
    LineCompGeometry geo;
    geo.start = m_start;
    geo.end = m_end;

    // 构建预览实体并设置几何数据
    // 校验和映射由 recomputeModelBlock 在 subViewportDraw 中统一触发（见 2.3.6）
    // 如果映射非法，绘制层根据 isMappingValid() 决定不显示（见 2.3.9）
    auto* pEnt = new ZcDbBmLineElement();
    pEnt->setGeometry(geo);

    replacePreviewEntity(pEnt);
    return Adesk::kTrue;
}
```

**sampler 职责**：获取鼠标当前点，与上一帧比较去重（返回 `kNoChange` 避免无效重绘）。
**update 职责**：基于最新输入数据，构建预览实体。校验不在此处执行 — `recomputeModelBlock` 在绘制时自动触发，内部完成预检 + 映射 + 状态设置，绘制层根据 `m_mappingValidity` 决定是否显示（见 2.3.9-2.3.10）。

#### 2.4.5 ArcCompJig：复用同一 doIt 流程

```cpp
class ArcCompJig final : public LinearCompJigBase {
public:
    explicit ArcCompJig(const LinearCompRuntimeContext& ctx);
    ~ArcCompJig() override;

    DragStatus sampler() override;
    ZSoft::Boolean update() override;

protected:
    bool acquireFixedPoints() override;
    const ZTCHAR* dynamicPrompt() const override { return _T("\n指定弧线构件第三点: "); }
    bool commit() override;

private:
    ZcGePoint3d m_start;
    ZcGePoint3d m_end;
    ZcGePoint3d m_third;
    ZcGePoint3d m_prevThird;
};

bool ArcCompJig::acquireFixedPoints() {
    if (zcedGetPoint(nullptr, _T("\n指定弧线构件起点: "), asDblArray(m_start)) != RTNORM) return false;
    if (zcedGetPoint(asDblArray(m_start), _T("\n指定弧线构件终点: "), asDblArray(m_end)) != RTNORM) return false;
    return true;
}
```

#### 2.4.6 入库阶段

`commit()` 统一做四件事：

1. 从 Jig 当前点位组装 `LineCompGeometry / ArcCompGeometry`，创建对应实体子类（`ZcDbBmLineElement / ZcDbBmArcElement`）。  
2. 强制触发 `recomputeModelBlock`（内部完成预检 + 映射 + 状态设置）。  
3. 查询 `isMappingValid()`：非法则弹 Error 对话框并取消入库（见 2.3.10）。  
4. 合法则调用 `appendZcDbEntity` 入库，失败则输出错误并回滚内存对象。

- 该阶段由各类型 Jig 的 `commit()` 实现；命令层只负责调度，不直接介入入库细节。

### 2.5 类型扩展复用框架（解决 LinearCompType 扩展复用性）

避免"每新增一种 `LinearCompType` 都改多处 `switch`"，采用**注册式内核**：

```cpp
struct LinearCompRuntimeContext;
class LinearCompJigBase;

class ILinearCompTypeKernel {
public:
    virtual ~ILinearCompTypeKernel() = default;
    virtual LinearCompType type() const = 0;
    virtual std::unique_ptr<LinearCompJigBase> createJig(
        const LinearCompRuntimeContext& ctx) const = 0;
    // 注：precheckRules() / commitRules() 已移除。
    // 轻量预检内聚到 ZcDbBmLinearElementImpl::precheckLocateInfo()（见 2.3.5），
    // 重量级校验由 mapInstanceModelBlk / mapUnBlockEnts 映射过程本身承担（见 2.3.7-2.3.8）。
    // Kernel 只负责 Jig 创建，不再管理校验规则。
};

class LinearCompTypeRegistry {
public:
    static LinearCompTypeRegistry& instance() {
        static LinearCompTypeRegistry s;
        return s;
    }

    void registerKernel(std::unique_ptr<ILinearCompTypeKernel> kernel) {
        auto type = kernel->type();
        m_kernelMap[type] = std::move(kernel);
    }

    const ILinearCompTypeKernel* find(LinearCompType type) const {
        auto it = m_kernelMap.find(type);
        return it != m_kernelMap.end() ? it->second.get() : nullptr;
    }

private:
    LinearCompTypeRegistry() = default;
    std::map<LinearCompType, std::unique_ptr<ILinearCompTypeKernel>> m_kernelMap;
};
```

命令注入点（统一调度入口）：

```cpp
class LinearCompJigFactory {
public:
    static std::unique_ptr<LinearCompJigBase> Create(LinearCompType type, const LinearCompRuntimeContext& ctx) {
        const ILinearCompTypeKernel* kernel = LinearCompTypeRegistry::instance().find(type);
        if (kernel == nullptr) return nullptr;
        return kernel->createJig(ctx);
    }
};
```

`CmdLinearCompByType(...)` 主流程只保留在 2.4.2，2.5 仅提供注入点与扩展约束，避免重复维护。

**改进点**：

- `LinearCompTypeRegistry::m_kernelMap` 改为 `private`，保护封装。
- 使用 `std::map` 替代 `std::unordered_map`，避免 `enum class` 需要自定义 hash 的问题。
- `ILinearCompTypeKernel` 不再包含 `createGeometry()`、`createStrategy()`、`precheckRules()`、`commitRules()` — 几何构造和策略创建由各实体子类内部完成（见 2.2.4、2.2.5 的 `createStrategy()`），校验由 `ZcDbBmLinearElementImpl` 内部承担（见 2.3.5-2.3.8），Kernel 只负责 Jig 创建。
- `instance()` 使用 Meyer's Singleton（C++11 标准保证线程安全的局部静态初始化）。

扩展约束与步骤：

- 新增类型（如 `kSpline`）需要：
  1. 新增 `SplineCompGeometry` 数据结构。
  2. 新增 `SplineCompStrategy` 映射策略。
  3. 新增 `ZcDbBmSplineElement` 实体子类（override 序列化/夹点/变换/`createStrategy`）。
  4. 新增 `SplineCompJig` Jig 子类。
  5. 新增 `SplineCompKernel` 并注册到 `LinearCompTypeRegistry`。
- 命令主流程（`CmdLinearCompByType`）、基类绘制框架均不改。
- 新增错误场景时，扩展 `EMappingSubCode` 枚举并在 `precheckLocateInfo()` 或 `mapInstanceModelBlk` 中新增对应分支即可（见 2.3.11）。

**样条曲线扩展示例**（混合方案下的 Geometry + Strategy 骨架）：

```cpp
struct SplineCompGeometry {
    std::vector<ZcGePoint3d> fitPoints;   // 拟合点序列
    int degree = 3;                       // 样条阶数
    bool closed = false;                  // 是否闭合

    double pathLength() const {
        return toGeCurve().length(toGeCurve().startParam(), toGeCurve().endParam());
    }

    // 按需构造 Ge 曲线
    ZcGeNurbCurve3d toGeCurve() const {
        return ZcGeNurbCurve3d(degree, ZcGePolyline3d(fitPoints), closed);
    }
};

class SplineCompStrategy final : public ILinearCompStrategy {
public:
    explicit SplineCompStrategy(std::shared_ptr<const SplineCompGeometry> geo)
        : m_geo(std::move(geo)) {}

    ZcGePoint3d mapPoint(const ZcGePoint3d& pt) const override
    {
        ZcGeNurbCurve3d spline = m_geo->toGeCurve();
        double totalLen = m_geo->pathLength();
        if (totalLen < 1e-10) return spline.evalPoint(spline.startParam());

        // 沿样条曲线按弧长参数化定位
        double targetLen = pt.x;
        double param = spline.paramAtLength(spline.startParam(), targetLen);
        ZcGePoint3d origin = spline.evalPoint(param);

        // 构建 Frenet 标架
        ZcGeVector3d tangent;
        spline.evalPoint(param, 1, &tangent);
        tangent.normalize();

        ZcGeVector3d zRef(0.0, 0.0, 1.0);
        if (tangent.isParallelTo(zRef)) zRef.set(0.0, 1.0, 0.0);
        ZcGeVector3d binormal = tangent.crossProduct(zRef).normal();
        ZcGeVector3d normal = binormal.crossProduct(tangent).normal();

        return origin + binormal * pt.y + normal * pt.z;
    }

    ZcGeNurbCurve3d pathCurve() const { return m_geo->toGeCurve(); }

private:
    std::shared_ptr<const SplineCompGeometry> m_geo;
};
```

样条构件序列化遵循同一模式 — 逐字段写入分量数据：

```cpp
// ZcDbBmSplineElement::dwgOutFields 骨架
pFiler->writeInt32(static_cast<int32_t>(m_geo.fitPoints.size()));
for (const auto& pt : m_geo.fitPoints)
    pFiler->writePoint3d(pt);
pFiler->writeInt32(m_geo.degree);
pFiler->writeBool(m_geo.closed);
```

### 2.6 设计模式分析

#### 2.6.1 当前采用的模式

| 模式 | 应用位置 | 作用 |
|------|---------|------|
| **策略模式 (Strategy)** | `IZcBmLinearMapAlgo` → `ZcBmLineMapAlgo` / `ZcBmArcMapAlgo` | 隔离不同布置方式的映射算法 |
| **模板方法 (Template Method)** | `LinearCompJigBase::doIt()`、`ZcDbBmLinearElement::subWorldDraw` + `rebuildCache` | 固定流程骨架，子类填充差异步骤 |
| **注册表 (Registry)** | `LinearCompTypeRegistry` + `ILinearCompTypeKernel` | 开闭原则，命令层无需 switch |
| **状态成员桥接** | `m_mappingValidity` + `setMappingFailed()` / `setMappingValid()` | 桥接 `recomputeModelBlock` 内部与上层（绘制 / Jig / commit）的分层响应 |
| **懒缓存 (Lazy Cache)** | `ZcDbBmLinearElement::m_cacheValid` + `rebuildCache()` | 避免重绘时重复映射计算 |
| **Memento (备忘录)** | `m_snapshot` + `rollback()` | 映射非法时自动恢复上次合法状态 |
| **递归防御** | `m_bInRollbackRecompute` | 防止 rollback → recompute 极端场景下的无限递归 |

**职责分配原则**：
- 映射算法的差异由 Strategy 隔离
- 实体数据/序列化/夹点/变换的差异由实体子类继承隔离
- 映射合法性由 `recomputeModelBlock` 内部设置（`m_mappingValidity`），上层通过查询接口读取并决定响应行为
- 弹窗、回退、不显示等交互行为始终在外部调用层，不侵入映射内核

**关于原 Pipeline 模式（`ValidationPipe`）的移除说明**：原方案中的 `ValidationPipe` + `IValidationRule` 框架设计为在映射外部独立调用。但实际实现中映射过程本身就是最权威的校验（映射失败 = 非法），独立的规则管道会导致校验逻辑分散且与映射结果不一致。改造后：轻量预检内聚到 `precheckLocateInfo()` 函数，重量级校验由映射过程本身承担，结果统一写入 `m_mappingValidity` 成员。

#### 2.6.2 访问者模式 (Visitor) 适用性分析

**结论：当前阶段不需要引入 Visitor。**

Visitor 模式适合的场景是：**数据结构（类型）稳定，但需要频繁新增操作**。例如编译器对 AST 节点（类型固定）执行不同遍历（代码生成、优化、类型检查等）。

本系统的情况恰好相反：

| 维度 | 实际情况 | Visitor 适配性 |
|------|---------|---------------|
| 类型稳定性 | `LinearCompType` 会扩展（Spline、Polyline...） | 差 — 每新增类型需修改所有 Visitor |
| 操作稳定性 | 操作基本固定（映射、校验、绘制、序列化） | 好 — 但已有策略模式充分覆盖 |

如果引入 Visitor，扩展一个新的 `LinearCompType` 需要同时修改**所有** Visitor 实现类，违反开闭原则。当前的 Registry + Strategy 方案更适合"类型可扩展、操作固定"的场景。

**何时考虑引入 Visitor**：如果未来样板中的实体类型（直线/圆弧/样条/面/体）稳定下来，且需要对同一套实体做多种不同的遍历操作（映射绘制、碰撞检测、面积计算、导出格式转换...），可以对**样板内实体**引入 Visitor，而非对 `LinearCompType`：

```cpp
// 仅在样板实体遍历层面考虑（未来需要时再引入）
class ITemplateEntityVisitor {
public:
    virtual ~ITemplateEntityVisitor() = default;
    virtual void visit(const TemplateLine& line) = 0;
    virtual void visit(const TemplateArc& arc) = 0;
    virtual void visit(const TemplateNurbs& nurbs) = 0;
};
```

#### 2.6.3 其他可考虑的模式

**Flyweight（享元）— 样板数据共享**

同一族类型可能被多个构件实例引用，样板数据（几何定义、属性信息）不应在每个实体中重复存储。当前设计通过 `m_familyTypeId` 引用族类型来间接实现了这一点，`rebuildCache()` 按需从族类型读取。建议进一步引入显式的样板缓存池：

```cpp
class TemplateDataCache {
public:
    static TemplateDataCache& instance();
    const TemplateData* get(const ZcDbObjectId& familyTypeId);
    void invalidate(const ZcDbObjectId& familyTypeId);
private:
    std::unordered_map<ZcDbObjectId, TemplateData> m_cache;
};
```

**Memento（备忘录）— 夹点编辑回退**

内网实现中，`m_snapshot` + `rollback()` 在 `ZcDbBmLinePlacementImpl` / `ZcDbBmArcPlacementImpl` 中实现。`moveGripPointsAt` 修改 `m_locateInfo` 后调用 `syncDataToProperty()` 触发 `recomputeModelBlock`，若映射非法则 `recomputeModelBlock` 内部自动调用 `rollback()` 恢复 `m_snapshot` 并用旧数据重算。调用方无需手动校验和回退（见 2.3.6）。

外网方案中的等价实现（展示概念）：

```cpp
Zcad::ErrorStatus ZcDbBmLinePlacementImpl::moveGripPointsAt(
    const ZcDbIntArray& indices, const ZcGeVector3d& offset)
{
    m_snapshot = m_locateInfo;  // Memento：值拷贝快照

    for (int i = 0; i < indices.logicalLength(); ++i) {
        switch (indices[i]) {
        case 0: m_locateInfo.ptStart += offset; break;
        case 1: m_locateInfo.ptEnd   += offset; break;
        case 2: m_locateInfo.ptStart += offset;
                m_locateInfo.ptEnd   += offset; break;
        }
    }

    // syncDataToProperty → recomputeModelBlock
    // 若映射非法，recomputeModelBlock 内部：
    //   rollback() → setLocateInfo(m_snapshot) → 递归重算
    // 整个过程对调用方透明
    syncDataToProperty();
    return Zcad::eOk;
}
```

### 2.7 **产品待明确问题**

- 样板文件中的扫掠/放样等特征在项目文件中的映射是哪种方式：
  - 样板文件中的截面按布置方式（直线/圆弧）进行几何映射，映射后所得结果再进行相同的特征处理
  - 样板文件中特征变换生成的实体直接通过几何映射的方式转换为项目文件中的实体
  - 镜像特征：是否也是直接对样板文件中镜像出的多实体直接通过几何映射的方式转换为项目文件中的多实体
- 需求2.3.4.2通用编辑中关于`**框选夹点**`的功能是否有必要，CAD是否存在这样的机制
- 需求2.5异常场景说明中关于，非法问题：
  - 路径长度不足以容纳样板最小实体：见 2.3.3 节说明
  - 布置过程中 Jig 预览：映射非法时所有实体不显示（绘制层 `isMappingValid()` 判断，见 2.3.9），不阻断鼠标交互
  - 布置过程中 commit 入库：映射非法时弹 Error 对话框，取消此次入库（见 2.3.10）
  - 拖动夹点过程中（编辑时）：`recomputeModelBlock` 内部自动 `rollback()` 恢复上次合法状态（见 2.3.6），对调用方透明
